# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Integration tests

on:
  push:
  pull_request:
  schedule: 
    - cron: '30 5 * * 1-5'

jobs:
  integration:
    runs-on: ubuntu-latest

    strategy:
      max-parallel: 10
      matrix:
        server: 
          - latest
#          - 4.14.5
          - 4.13
        sdk: [
          {container: node, port: 3000, label: 'NodeJS'},
          {container: python, port: 5001, label: 'Python'},
          {container: java, port: 5100, label: 'Java'},
        ]

    steps:
      - uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
          cache: "npm"
      - run: yarn install
      - name: Setup unleash server
        run: UNLEASH_SERVER_VERSION=${{ matrix.server }} docker compose up -d postgres unleash-server
      - name: Await initialization
        run: ./wait-unleash-server-has-token.sh

      - name: Test ${{ matrix.sdk.container }} SDK with ${{ matrix.server }}
        run: ./run-test.sh ${{ matrix.sdk.container }} ${{ matrix.sdk.port }} ${{ matrix.sdk.label }}

      - name: Clean up
        run: docker compose stop postgres unleash-server