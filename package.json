{
  "scripts": {
    "test": "yarn test:node && yarn test:java && yarn test:python",
    "test:node": "SDK_URL=http://localhost:3000 SDK_LABEL=NodeJS jest",
    "test:java": "SDK_URL=http://localhost:5100 SDK_LABEL=Java jest",
    "test:python": "SDK_URL=http://localhost:5001 SDK_LABEL=Python jest"
  },
  "devDependencies": {
    "@types/got": "^9.6.12",
    "@types/jest": "^27.5.0",
    "@unleash/client-specification": "^4.2.2",
    "jest": "^28.0.3",
    "ts-jest": "^28.0.1",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "got": "^11.8.5"
  }
}
